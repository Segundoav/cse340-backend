<%- include('partials/header') %>
    <main>
        <h1><%= title %></h1>
        
        <p>Here are our project categories:</p>
        <ul>
            <% categories.forEach(category => { %>
                <li>
                    <strong><%= category.category_name %></strong>
                </li>
            <% }); %>
        </ul>
    </main>
<%- include('partials/footer') %>